/** What a status asks of the reader: act (caution), wait (progress), nothing (positive, neutral), or notice a failure. */
export type Tone = "positive" | "caution" | "negative" | "neutral" | "progress";
