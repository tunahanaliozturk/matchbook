<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Matchbook.Requisitions.Domain\Matchbook.Requisitions.Domain.csproj" />
    <ProjectReference Include="..\..\..\Shared\Matchbook.Contracts\Matchbook.Contracts.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Provider-neutral EF Core only: DbSet and async LINQ, plus Relational for AsSplitQuery, since a requisition
      loads three child collections and one joined query would multiply them. The provider, migrations and
      naming live in Infrastructure.
    -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Relational" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Matchbook.Requisitions.UnitTests" />
    <InternalsVisibleTo Include="Matchbook.Requisitions.IntegrationTests" />
  </ItemGroup>

</Project>
